"""
desktop — the window that makes this an application rather than a localhost tab.

`python -m desktop` is the entry point; see `__main__.py`. `python -m server`
runs the same API with no window, which is what `npm run dev` proxies against.
"""
